# 4.29 AutoWeb 接入 dp_cli 计划

生成日期：2026-04-29

## 目标

将 AutoWeb 从“Coder 生成 DrissionPage Python 代码并由 Executor 动态执行”的模式，逐步替换为“Coder/Planner 生成结构化 dp_cli 动作，由 Executor 统一调用 dp_cli 执行”的模式。

核心目标不是立刻重写 AutoWeb，而是先用最小改动接入 dp_cli，让常见网页自动化和爬取任务优先走更稳定、更安全、更低成本的 ref-driven CLI 流程。

## 总体判断

当前不建议先把 dp_cli 合并进 AutoWeb 后再改。

推荐方案：

- 短期：保持两个库独立，在 AutoWeb 新增 dp_cli 适配层。
- 中期：AutoWeb 支持双轨执行，普通动作走 dp_cli，特殊任务保留旧 Python Coder。
- 长期：稳定后可考虑 monorepo，但仍保持 `dp_cli` 作为独立 package 边界。

不推荐方案：

- 不建议让 Coder 写 `subprocess.run(["python", "-m", "dp_cli", ...])` 这类 Python 代码。
- 不建议把 dp_cli 直接揉进 AutoWeb 的 `skills/` 或 `core/`。
- 不建议一口气删除旧 Coder/Executor。

## 接入后的目标架构

```text
AutoWeb
  Planner
    -> 决定下一步任务
  Observer
    -> 逐步改为读取 dp_cli snapshot
  Coder
    -> 生成 dp_cli action JSON
  Executor
    -> 调用 DPCLIExecutor 执行动作
  Verifier
    -> 根据 dp_cli 返回 JSON + 当前页面状态验收
  Cache
    -> 从缓存 Python 代码逐步改为缓存 dp_cli action/action plan

dp_cli
  open / snapshot / find / click / type / extract / batch-detail-extract
  session / ref / semantic snapshot / browser runtime
```

## 第一阶段：外部库接入，不合并代码

目标：AutoWeb 能通过适配层调用当前仓库里的 dp_cli。

建议改动 AutoWeb：

1. 新增 `skills/dpcli_executor.py`
2. 封装 `DPCLIExecutor`
3. 支持以下方法：
   - `open(url)`
   - `snapshot(mode="agent_summary", ref=None, depth=None)`
   - `find(text=None, locator=None)`
   - `click(ref=None, locator=None)`
   - `type_text(ref, text)`
   - `expand(ref, depth=2)`
   - `extract(target_ref, schema=None, limit=None)`
   - `batch_detail_extract(items, ...)`
4. 所有方法统一返回 dp_cli JSON：

```json
{
  "ok": true,
  "session": "autoweb",
  "action": "click",
  "data": {},
  "error": null
}
```

建议执行方式：

```python
cmd = ["python", "-m", "dp_cli", *args, "--session", session]
```

注意：

- 这段 `subprocess` 只能写在 AutoWeb 受控适配层里。
- 不允许 LLM 生成 `subprocess` 代码。
- 旧 `code_guard` 仍然保留，用于旧 Python Coder 模式。

## 第二阶段：新增 dp_cli action 模式

目标：AutoWeb Coder 不再只生成 Python，也可以生成结构化动作。

新增状态字段建议：

```python
generated_action: Optional[Dict[str, Any]]
execution_mode: Optional[str]  # "python_code" | "dp_cli"
dpcli_session: Optional[str]
```

Coder 输出格式：

```json
{
  "thought": "当前页面已有搜索输入框和搜索按钮，先输入关键词。",
  "action": {
    "skill": "type",
    "params": {
      "ref": "e12",
      "text": "python tutorial"
    },
    "reason": "搜索框 ref 已在 snapshot 中出现"
  }
}
```

Executor 分支：

```text
if execution_mode == "dp_cli":
    执行 generated_action
else:
    执行旧 generated_code
```

推荐先支持单步 action，不要一开始支持长 action list。单步更容易验收、回滚和重试。

## 第三阶段：Observer 逐步切换到 dp_cli snapshot

目标：减少 AutoWeb 自己的 DOM skeleton + locator prompt 负担。

优先使用：

```bash
python -m dp_cli snapshot --mode agent_summary --session autoweb
```

传给 Planner/Coder 的内容不要是完整 snapshot，而是裁剪后的 agent view：

- 当前 URL/title
- page identity
- interactable elements
- surface nodes
- 少量 data regions
- 最近 action result
- 最近失败原因

保留旧 Observer：

- 当 dp_cli snapshot 无法识别目标时，可 fallback 到旧 Observer。
- 复杂页面、登录态异常、强动态 DOM 页面，先不要强制切换。

## 第四阶段：缓存从代码缓存改为动作缓存

旧 CodeCache：

```text
goal + url + locator_info -> Python code
```

新 ActionCache：

```text
goal + url + snapshot_signature + task_type -> dp_cli action/action plan
```

示例缓存：

```json
{
  "task": "点击下一页",
  "url_pattern": "example.com/list",
  "action": {
    "skill": "find",
    "params": {"text": "下一页"}
  },
  "next_action_hint": {
    "skill": "click",
    "params_from_previous": "first_result_ref"
  }
}
```

迁移策略：

- 短期保留旧 CodeCache。
- 新增 ActionCache，不强行迁移旧缓存。
- 验证成功的 dp_cli action 再写入 ActionCache。
- 连续失败的 action cache hit 进入失败黑名单。

## 第五阶段：爬取链路优先替换

优先替换的任务类型：

1. 打开 URL
2. 搜索关键词
3. 点击分类/筛选/下一页
4. 列表页提取
5. 详情页批量提取

推荐 dp_cli 流程：

```text
open
snapshot
extract list region
find/click next page
snapshot
extract list region
batch-detail-extract
Verifier
RAG store
```

详情页任务不要让 LLM 每个详情页单独推理。优先使用：

```bash
python -m dp_cli batch-detail-extract
```

## 预期收益

1. 成本降低
   - Coder prompt 变短。
   - 输出从 Python 代码变成 action JSON。
   - 很多失败可以规则化恢复，不需要再次调用 LLM 改代码。

2. 稳定性提升
   - 点击、输入、抽取走统一实现。
   - ref stale、invalid ref、element not found 可标准化处理。
   - 减少 LLM 随机生成错误 DrissionPage 写法。

3. 安全性提升
   - 不再默认执行任意 Python。
   - HITL 可以从审代码变成审动作。
   - subprocess/open/os/exec 等高风险能力只存在于受控适配层。

4. 调试更清楚
   - AutoWeb 负责决策。
   - dp_cli 负责执行。
   - 出错时能区分是 action 选错、ref 失效，还是 dp_cli 执行能力不足。

## 主要风险

1. 灵活性下降
   - 动态 Python 能做的事情更多。
   - dp_cli 未暴露的能力需要新增 CLI 命令或 fallback 到旧模式。

2. 浏览器生命周期冲突
   - AutoWeb 原本自己管理 DrissionPage browser。
   - dp_cli 也管理 session/profile/port。
   - 接入时必须明确：dp_cli 模式下由 dp_cli 接管浏览器。

3. 快照 token 成本仍需控制
   - 不能把 full snapshot 每轮直接塞给 LLM。
   - 必须使用 `agent_summary` 和裁剪视图。

4. 缓存体系需要重做
   - 旧 CodeCache 不能直接等价复用。
   - 需要新增 action cache 或双轨缓存。

5. dp_cli 命令能力还要补齐
   - wait/scroll/select/upload/download 等命令后续可能需要新增。
   - 否则复杂场景会过度依赖 `eval`。

## 建议给 Codex/opencode 的第一条任务

```text
在 AutoWeb 中新增 dp_cli 执行适配层，不改 dp_cli 源码。

要求：
1. 新增 skills/dpcli_executor.py
2. 实现 DPCLIExecutor，封装 open/snapshot/find/click/type/expand/extract/batch_detail_extract
3. 所有方法调用 python -m dp_cli，并返回解析后的 JSON
4. 新增最小单元测试或 smoke 脚本，验证 open + snapshot 能跑通
5. 不改现有 Coder/Executor 主流程
```

## 建议给 Codex/opencode 的第二条任务

```text
给 AutoWeb 增加 dp_cli action 执行模式，保留旧 Python Coder 模式。

要求：
1. 新增 generated_action / execution_mode 状态字段
2. 新增 dp_cli action prompt，要求 LLM 只输出 JSON
3. Executor 中增加 execution_mode == "dp_cli" 分支
4. 支持 open/snapshot/find/click/type/extract 六个基础动作
5. action 执行失败时写入 execution_log 和 verification_result
6. 旧 generated_code 执行逻辑保持不变
```

## 建议里程碑

### M1：只接执行器

验收标准：

- AutoWeb 可以通过 `DPCLIExecutor` 调用 dp_cli。
- `open + snapshot` 成功。
- 不影响旧 AutoWeb 流程。

### M2：单步 action 模式

验收标准：

- Coder 输出 action JSON。
- Executor 能执行 `click/type/find/extract`。
- Verifier 能基于 dp_cli result 做判断。

### M3：列表爬取走 dp_cli

验收标准：

- 能完成一个列表页抽取任务。
- 能翻页后继续抽取。
- 能保存结构化数据。

### M4：详情页批量抽取

验收标准：

- 列表页先收集 detail URL。
- 使用 `batch-detail-extract` 批量抽取详情。
- 不让 LLM 对每个详情页单独循环推理。

### M5：缓存动作化

验收标准：

- 成功 action 可写入 ActionCache。
- 命中后可复用。
- 失败后可黑名单或降级到重新规划。

## 最终建议

先独立接入，不合并。

优先做薄适配层和双轨模式，让 dp_cli 承接常见浏览器动作和爬取执行。等 action 接口稳定、AutoWeb 旧 Python Coder 使用频率明显下降后，再考虑把两个项目放入同一个 monorepo，但仍保持模块边界清晰。
